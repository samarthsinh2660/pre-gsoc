{
  "version": 2,
  "builds": [
    {
      "src": "api/analyze.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    }
  ],
  "env": {
    "GITHUB_TOKEN": "@github_token"
  }
}
